package entity

/** Enum to distinguish between squares on the gameboard */
enum class Color {
    BLUE,
    YELLOW,
    RED,
    GREEN,
    BLANK,
    ADJACENT,
    DIAGONAL,
    BLOCKED,
    CORNER
}